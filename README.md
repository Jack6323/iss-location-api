# ISS Tracker

This project fetches the **current location of the International Space Station (ISS)** using a public API. It saves the data to a JSON file and prints latitude and longitude.

## How to Run

```bash
git clone https://github.com/YOUR_USERNAME/iss-location-api.git
cd iss-location-api
pip install requests
python ISS_Tracker.py
```
